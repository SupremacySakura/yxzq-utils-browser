import { uploadResource, getFilePath } from './resource-storage-utils/index'
import { checkIfInstanceOf, debouncing, throtting } from './utils/index'
export { uploadResource, getFilePath }
export { checkIfInstanceOf, debouncing, throtting }
const yxzqUtils = {
    uploadResource,
    getFilePath,
    checkIfInstanceOf,
    debouncing,
    throtting
}
export default yxzqUtils
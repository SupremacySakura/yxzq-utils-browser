import { uploadResource, getFilePath } from './resource-storage-utils/index'
import { checkIfInstanceOf, debouncing, throtting, addCacheToAxios } from './utils/index'
export { uploadResource, getFilePath }
export { checkIfInstanceOf, debouncing, throtting, addCacheToAxios }
const yxzqUtils = {
    uploadResource,
    getFilePath,
    checkIfInstanceOf,
    debouncing,
    throtting,
    addCacheToAxios
}
export default yxzqUtils